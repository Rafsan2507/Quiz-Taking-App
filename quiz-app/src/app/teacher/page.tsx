import ShowCreatedQuizzes from "@/components/TeacherComponent/ShowCreatedQuizzes";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <ShowCreatedQuizzes />;
};

export default page;
